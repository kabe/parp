ワークフローアプリケーションを PARPDB に追加する際の手順

CRT は常に新規作成する
NCR はなければ作成する

* TABLE WORKFLOW
workflow_name = ??? どこかで取得
workflow_id = NCR(workflow.id=workflow_name)

* TABLE WORKFLOW_CONDITION
filesystem = CMDLINE ??? データを配置しているFilesystem
location = CMDLINE ??? クラスタ名 or 環境名
worker_num = CMDLINE ??? ワーカー数 gxpmake DB から取れる？
workflow_condition_id = NCR(workflow_condition.id=(filesystem, location, worker_num))

* TABLE WORKFLOW_TRIAL
workflow_trial_id = CRT(workflow_trial_id=(workflow_id, workflow_condition_id))

* TABLE APPLICATION
application_name = GXPDB から取得
application_id = workflow_id 中の最大値 + 1 (NCR)

* TABLE JOB
残りデータを GXPDB から取得
job_id = CRT(全部)

* TABLE METRIC

metric_id = CRT
